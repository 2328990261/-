# 轻小说推荐系统 · E-R 图绘制示例（供毕设手绘 / Visio 参照）

以下内容按 **Chen 表示法**：**矩形 = 实体**，**菱形 = 联系**，连线上标 **1、n、m**；**不在图里写属性**，与论文常见要求一致。

---

## 一、建议保留的主干（够写「数据库概念设计」一节）

| 实体（矩形） | 对应数据表 | 说明 |
|-------------|------------|------|
| 用户 | `user` | 注册、登录、行为主体 |
| 小说 | `novel_book_main` | 书目主记录 |
| 分卷 | `novel_book_volume` | EPUB 分卷正文 |
| 标签 | `tag` | 平台标签与推荐权重 |
| 轮播图 | `banner_carousel` | 首页轮播，可挂某本小说 |

| 联系（菱形） | 两端实体 | 基数 | 实现要点（写论文时可一句话带过） |
|-------------|----------|------|----------------------------------|
| **交互** | 用户 — 小说 | **m : n** | 收藏、评论、阅读历史、完读、不感兴趣等多张表，均含 `user_id` 与 `novel_id`（或等价） |
| **包含** | 小说 — 分卷 | **1 : n** | `novel_book_volume.main_book_id` → `novel_book_main.id` |
| **关联** | 小说 — 轮播图 | **1 : n** | `banner_carousel.novel_id` 可空，多图可指向同一小说 |
| **用于分类与推荐** | 标签 — 小说 | **m : n（概念）** | 库中多为 `novel_book_main.label` 文本与 `tag.name` 逻辑对应，**无外键**；图上可画虚线或脚注说明「逻辑关联」 |

若篇幅紧，可**省略「轮播图」**或**省略「标签—小说」这条概念联系**，只保留「用户—小说—分卷」核心三角。

---

## 二、示例拓扑（怎么摆、线怎么连）

把 **「小说」放在中间**，其余实体围一圈，菱形插在两条实体之间：

```
                         m              n
           ┌──────┐      ◇ 交互 ◇      ┌──────┐
           │ 用户 │ ────────────────── │ 小说 │
           └──────┘                    └──┬───┘
                                          │ 1
                                          │      ◇ 包含 ◇
                                          │ n
                                     ┌────┴────┐
                                     │  分卷   │
                                     └─────────┘

           ┌────────┐   1    ◇ 关联 ◇    n   ┌────────┐
           │ 小说   │ ──────────────────────── │ 轮播图 │
           └────────┘                        └────────┘

                         m              n
           ┌──────┐      ◇ 用于分类与推荐 ◇   ┌──────┐
           │ 标签 │ ──────────────────────── │ 小说 │
           └──────┘                          └──────┘
```

手绘时：**同一实体只画一个矩形**（上面「小说」出现多次是为了分块示意，你合并成一个框、多拉几条线即可）。

---

## 三、Visio / Word 操作建议（避免乱）

1. 先拖 **5 个矩形**、**4 个菱形**，按上表改名。  
2. 用 **连接线** 连「实体—菱形—实体」，在靠近两端处手写或插入文本 **1、n、m**。  
3. **标签—小说** 若学校要求「只画有外键的联系」，可**不画**该菱形，在正文说明「标签通过书目标签字段参与推荐」。  
4. 图下加一句：**「交互」在逻辑上为多对多，物理实现由多张行为表完成。**

---

## 四、与毕设题目的对应话术（可摘一句进正文）

> 本系统在概念层将「用户—书目」抽象为多对多联系 **交互**，涵盖收藏、评论、阅读与完读等行为；书目与 **分卷** 为一对多 **包含** 关系，支撑 EPUB 分卷存储与阅读；**标签** 与书目在推荐与检索层面形成多对多 **用于分类与推荐** 关系（实现上通过标签名字段与书目标签文本对应）。

---

按此示例自己画一版即可；若学校模板要求 **IDEF1X** 或 **UML 类图** 而不是 Chen，可以说一下我按同一套表给你改一版「方框+乌鸦脚」示例结构。
