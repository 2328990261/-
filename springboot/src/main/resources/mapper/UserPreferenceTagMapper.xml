<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wangrui.springboot.mapper.UserPreferenceTagMapper">

    <!-- 结果映射 -->
    <resultMap id="UserPreferenceTagResultMap" type="com.wangrui.springboot.pojo.UserPreferenceTag">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tagName" column="tag_name"/>
        <result property="tagType" column="tag_type"/>
        <result property="tagOrder" column="tag_order"/>
        <result property="isFirstRow" column="is_first_row"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 获取用户偏好标签列表 -->
    <select id="selectByUserId" parameterType="java.lang.Integer" resultMap="UserPreferenceTagResultMap">
        SELECT id, user_id, tag_name, tag_type, tag_order, is_first_row, created_at, updated_at
        FROM user_preference_tag
        WHERE user_id = #{userId}
        ORDER BY tag_order ASC
    </select>

    <!-- 根据类型获取用户偏好标签列表 -->
    <select id="selectByUserIdAndTagType" resultMap="UserPreferenceTagResultMap">
        SELECT id, user_id, tag_name, tag_type, tag_order, is_first_row, created_at, updated_at
        FROM user_preference_tag
        WHERE user_id = #{userId} AND tag_type = #{tagType}
        ORDER BY tag_order ASC
    </select>

    <!-- 保存用户偏好标签 -->
    <insert id="insert" parameterType="com.wangrui.springboot.pojo.UserPreferenceTag">
        INSERT INTO user_preference_tag (user_id, tag_name, tag_order, is_first_row, created_at, updated_at)
        VALUES (#{userId}, #{tagName}, #{tagOrder}, #{isFirstRow}, NOW(), NOW())
    </insert>

    <!-- 批量保存用户偏好标签 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO user_preference_tag (user_id, tag_name, tag_type, tag_order, is_first_row, created_at, updated_at)
        VALUES
        <foreach collection="tags" item="tag" separator=",">
            (#{tag.userId}, #{tag.tagName}, #{tag.tagType}, #{tag.tagOrder}, #{tag.isFirstRow}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- 删除用户所有偏好标签 -->
    <delete id="deleteByUserId" parameterType="java.lang.Integer">
        DELETE FROM user_preference_tag
        WHERE user_id = #{userId}
    </delete>
    
    <!-- 删除用户指定类型的偏好标签 -->
    <delete id="deleteByUserIdAndTagType">
        DELETE FROM user_preference_tag
        WHERE user_id = #{userId} AND tag_type = #{tagType}
    </delete>

    <!-- 删除指定偏好标签 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM user_preference_tag
        WHERE id = #{id}
    </delete>

    <!-- 更新用户偏好标签 -->
    <update id="update" parameterType="com.wangrui.springboot.pojo.UserPreferenceTag">
        UPDATE user_preference_tag
        SET tag_name = #{tagName},
            tag_order = #{tagOrder},
            is_first_row = #{isFirstRow},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
