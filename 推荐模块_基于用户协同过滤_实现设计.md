# 推荐模块：基于用户的协同过滤 — 实现设计说明

本文档供后续开发使用。当前仓库中的协同分支为**基于物品**的共现协同（Item-based，书—书共现）；若论文与产品表述改为「标签兴趣相近的用户 → 取其收藏等行为加分」，应对齐为**基于用户**的协同过滤（User-based）。以下按目标算法、数据、接口改造、性能与论文表述分项说明。

---

## 1. 概念对齐

| 项目 | 基于用户（目标） | 基于物品（当前实现） |
|------|------------------|----------------------|
| 相似性 | 用户—用户（如标签权重向量余弦） | 物品—物品（共现 + 余弦型归一化） |
| 传播来源 | 近邻用户产生过互动的作品 | 当前用户种子书的共现邻居 |
| 典型 SQL 思路 | 找 Top-K 相似用户 → 拉取这些用户的收藏/互动 | 对每本种子书查 `selectCooccurItems` |

混合推荐整体不变：**内容分（标签） + λ×协同分 + 流行度**，第四步仍可为 MMR。仅**协同分的计算方式**由 Item-CF 替换为 User-CF。

---

## 2. 目标算法（User-based CF）

### 2.1 符号

- 目标用户 \(u\)；候选近邻用户 \(v \in \mathcal{N}_u\)（不含自身）。
- 用户 \(u\) 在标签空间上的有效权重向量 \(\mathbf{w}_u\)（与主路径一致：行为统计 + 覆盖 + 不感兴趣惩罚后的**有效占比**，标签名为维度，缺失为 0）。
- 用户相似度：建议 **余弦相似度**  
  \[
  \mathrm{sim}(u,v)=\frac{\sum_t w_{u,t}\,w_{v,t}}{\|\mathbf{w}_u\|\,\|\mathbf{w}_v\|}
  \]
  向量过稀疏时可设**最小共同标签数**或**最低 sim 阈值**，过滤不可靠近邻。
- 近邻集合：按 \(\mathrm{sim}(u,v)\) 降序取 **Top-K**（K 可配置，如 20～50），且可要求 \(v\) 有足够行为（避免僵尸账号）。

### 2.2 近邻对作品的贡献

对候选书 \(j\)（未在 \(u\) 的排除集合内），定义近邻 \(v\) 对书 \(j\) 的**隐式强度** \(r_{v,j}\)，建议与现 `buildUserSignals` 思想一致，但对象是**邻居 \(v\)**，例如：

- 收藏：+1（或现 `wCollection`）
- 完读：+1（或 `wFinished`）
- 评论： capped 次数 × `wComment`
- 阅读历史：进度与对数时长 × 对应权重  

则 User-CF 原始分为：

\[
S_{\mathrm{user\text{-}cf}}(j)=\sum_{v\in\mathcal{N}_u}\mathrm{sim}(u,v)\cdot r_{v,j}.
\]

仅对「\(r_{v,j}>0\)」的书累加；\(u\) 已收藏/已完读/不感兴趣的书在入候选前已过滤，与现逻辑一致。

### 2.3 与混合公式衔接

保持与 `RecommendController` 一致：在**当前批候选**上对 \(S_{\mathrm{tag}}\) 与 \(S_{\mathrm{user\text{-}cf}}\) 分别做最大值归一化得 \(\hat{S}_{\mathrm{tag}}\)、\(\hat{S}_{\mathrm{cf}}\)（变量名可仍叫 `cfScore` 以减少前端改动），再：

\[
S_{\mathrm{final}}=(1-\lambda)\hat{S}_{\mathrm{tag}}+\lambda\hat{S}_{\mathrm{cf}}+\beta\cdot\mathrm{pop}.
\]

---

## 3. 用户向量 \(\mathbf{w}_u\) 的来源

与主推荐路径一致，避免「内容用一套向量、协同用另一套」：

- **优先复用**现有「有效标签权重」计算结果（与 `UserRecommendProfileController.getUserTagWeights` / `computeEffectiveUserTagWeights` 同源）。
- 若某用户向量为空或范数为 0：不进入 User-CF，协同分为 0 或走冷启动（与现逻辑一致）。

---

## 4. 后端实现要点（改造清单）

### 4.1 新增/调整的数据访问

1. **批量拉取全量（或增量）用户的标签权重**  
   - 实现方式 A（简单）：请求时实时对最多 N 个用户算权重（易慢）。  
   - 实现方式 B（推荐）：定时任务或登录/行为变更时写入 `user_tag_vector` 快照表（user_id, tag_name, weight），查询时只读表 + 内存算 sim。  
2. **给定近邻用户 ID 列表，批量查询其对各书的隐式强度**  
   - 可复用现有表：`user_collection`、`user_finished_novel`、`user_comment`、`user_reading_history`，在内存中聚合为 \(r_{v,j}\)。  
   - SQL 宜按 `user_id IN (...)` 批量查，避免逐用户 N+1。

### 4.2 核心流程（替换 `aggregateItemCfScores`）

伪代码：

```
输入: 当前 userId u, 有效向量 w_u
1. 加载候选用户集合 V（全体活跃用户或「与 u 有至少一个共同标签」的子集以裁剪）
2. 对每个 v != u，计算 cos_sim(w_u, w_v)，取 Top-K 得 N_u
3. 批量查询 N_u 中所有用户的互动，构建 r_{v,j}
4. 对每个在候选库中的书 j: cfScore[j] = sum_{v in N_u} sim(u,v) * r_{v,j}
5. 返回 cfScore Map（及可选 explain：贡献最大的近邻用户 Top3）
```

### 4.3 `RecommendController` 修改点

- 将 `aggregateItemCfScores` + `mergeCooccurFromSeed` 替换为 `aggregateUserCfScores`（新写）。
- `CfBundle` 可保留：`scoreByNovel` 仍为 `Map<Integer,Double>`；`seedContribByNovel` 可改为 `Map<Integer, Map<Integer,Double>>` 表示 **novelId → { neighborUserId → contribution }**，供 `_explain` 使用。
- `coldStartRecommend` 里若仍用 `aggregateGlobalColdStartCf`，属**物品共现冷启动**；可保留作兜底，或改为「新用户无向量时用热门 + 标签兜底」，与文档 5.5.3 一致即可。

### 4.4 `RecommendCFMapper` / SQL

- 现有 `selectCooccurItems` 为 Item-CF 专用；User-CF 可**新增 Mapper 方法**，例如：  
  - `selectUserIdsWithMinInteractions(int minCount)`  
  - `selectInteractionsByUserIds(List<Integer> userIds)`  
  或分表查询后在 Java 层合并为 \(r_{v,j}\)。

### 4.5 可解释性 `_explain`

- `cfTopSeeds` 可改为 `cfTopNeighbors`：`[{ neighborUserId, contribution }]`。  
- 前端「别人在看」文案可改为 **「兴趣相近的读者也在读」**（若产品允许展示匿名统计，勿暴露具体用户 ID）。

---

## 5. 性能与风险

- **复杂度**：若每次对全站用户算 sim，\(O(|U| \cdot |T|)\)，用户量大时必须**裁剪候选邻居**（共同标签、同分区、或采样）。  
- **隐私**：近邻用户 ID 不可下发前端；explain 仅汇总分数或脱敏描述。  
- **一致性**：邻居向量需与主路径标签权重同源、同版本，否则混合意义模糊。

---

## 6. 论文表述（与实现一致后采用）

**（二）可选的协同过滤（基于用户）**  

开启时，系统根据与主路径一致的有效标签权重构造用户兴趣向量，在用户集合上计算**用户间相似度**（如余弦相似度），选取与当前用户**标签偏好最接近的 Top-K 位近邻**；再汇总这些近邻用户在收藏、完读、评论及阅读历史中的**隐式反馈**，对近邻曾产生较强互动的、且当前用户尚未排除的作品加权累加，得到**协同分**。其含义是：兴趣画像相近的读者群体所偏好的作品获得更高加分。推荐页可在存在协同分项时提示「兴趣相近的读者也在读」等，与基于物品共现的「与本书常被一并阅读的作品」表述区分。

---

## 7. 迁移建议

1. 配置项增加 `cf_mode: item | user` 便于 A/B 与回滚。  
2. 先实现 User-CF + 小数据量压测，再决定是否下线 Item-CF SQL。  
3. 更新 `毕业论文_系统说明.md` 或论文 5.5.2 中与协同过滤相关的段落，避免再写「书—书共现」。

---

## 8. 当前代码索引（便于定位）

| 内容 | 位置 |
|------|------|
| 物品共现 SQL | `springboot/.../mapper/RecommendCFMapper.xml` → `selectCooccurItems` |
| 协同分聚合 | `RecommendController.aggregateItemCfScores` / `mergeCooccurFromSeed` |
| 用户行为种子（现用于 Item-CF） | `RecommendController.buildUserSignals` |
| 标签权重 | `UserRecommendProfileController.computeEffectiveUserTagWeights` 等 |
| 前端「别人在看」 | `RecommendView.vue` → `showOthersWatching`（`cfScore > 0`） |

---

*文档版本：与仓库当前实现对照整理；实施 User-CF 后请同步更新本节与论文。*
