# 待完成事项

本文档记录项目中计划完成、尚未落地或暂缓的事项，便于后续迭代与答辩说明。

---

## 1. 用户密码：数据库中明文改为加密存储

**背景：**  
当前代码中，**新用户注册**已在 `UserServiceImpl.register` 里使用 `BCryptPasswordEncoder` 对密码加密后再写入数据库；**登录**时对以 `$2a$` 开头的密文走 `matches` 校验，对历史明文仍有一次兼容比对。

**问题：**  
若数据库中部分账号（如测试阶段手工插入的 `admin` / `root`）密码仍为**明文**（如 `123456`），与「密码应加密存储」的设计不一致，也存在安全隐患。

**待完成：**

- [ ] **方案 A（推荐）：** 用户登录成功且当前库中密码为明文时，用 BCrypt 加密后**回写**该用户 `password` 字段，实现逐账号自动迁移。
- [ ] **方案 B：** 编写一次性 SQL 或管理脚本，将已知明文的测试账号批量更新为 BCrypt 密文（需为每个明文预生成对应哈希，且仅适用于确定密码内容的行）。
- [ ] 迁移完成后，可视情况**收紧登录逻辑**：仅允许 BCrypt 校验，去掉明文兼容分支（需确认无遗留明文）。

**相关代码位置：**  
`springboot/.../service/impl/UserServiceImpl.java`（`login` / `register`）、`UserMapper.xml` 用户表更新。

---

## 2. 个性化推荐：论文/产品与当前实现的差异（待对齐）

**当前实现（以代码为准）：**

- **首页（`HomeView`）**：加载书库数据后，在前端按 **`readCount`（阅读量）降序** 排序；可选 **标签多选筛选**（`getNovelsByLabels`）。**同一套规则面向所有用户，不是按账号生成的「个性化推荐」**。
- **个性化推荐**：在 **`/recommend` 推荐页**、用户 **登录后** 调用 `/api/recommend/books`（结合偏好标签、评论热度、阅读历史等）时体现。未登录时推荐页展示逻辑接近书库（标签 + 列表），并非用户级推荐。

**论文撰写注意：**

- **不要**写「首页展示个性化推荐列表」或「首页根据用户行为生成推荐」，除非后续改造首页接入推荐接口。
- 宜区分表述：**首页** = 热门/阅读量排序 + 标签筛选；**推荐页（登录）** = 个性化推荐能力落点。
- 若摘要、研究内容、第 5 章等仍按模板写「首页 + 协同过滤」，需整体改为与上表一致，或列入下栏作为**待开发**后再写进「实现」章节。

**可选后续开发（真正对齐「首页即个性化」时再做）：**

- [ ] 首页改为请求推荐接口（需登录态与冷启动策略），或保留首页为热门、仅在文中改表述。

---

## 3. 小说检索：关键词搜索能力边界（论文勿夸大）

**当前实现：**

- 书库/导航栏关键词搜索走后端 `selectNovelsByKeyword`：**仅对书名、作者做模糊匹配**；关键词为**纯数字**时尝试按**作品 ID** 精确查。
- **不支持**在搜索框内用关键词去匹配 **`label`（标签/题材）字段**。
- **标签**通过首页/书库的 **「标签栏多选」** 单独筛选，与搜索框是两条能力。

**论文撰写注意：**

- **不要**写「关键词可搜索标签」或「按标签关键字检索」，除非以后扩展 SQL（例如在 `WHERE` 中增加对 `label` 的 `LIKE`）。
- 可写：按书名、作者（及编号）检索；按题材/标签筛选通过标签栏实现。

---

## 4. 推荐算法：与「协同过滤」经典实现的差异及后续计划

**当前实现（以 `RecommendController` 为准，非典型 User-CF / Item-CF）：**

- **用户侧输入**：从个人中心/收藏推导的偏好标签，或「自定义排序」类标签；参与加权的标签数量有上限（如前 5 个）。
- **标签权重来源**：每个标签的权重来自 **`tag` 表** 中由管理员配置的 **`recommend_weight`（全站统一）**，通过 `TagService.getRecommendWeights()` 读取；**不会**随某一用户的收藏、评论、阅读次数自动变化。
- **打分规则（对书库中每本书）**：
  - 将作品 `label` 拆成标签，与用户偏好标签**命中则累加对应权重**；
  - 可选：按全站**评论条数**加分（有上限 cap，防刷分）；
  - 可选：若该书出现在该用户**最近 N 条阅读历史**（去重）中，则加固定分项；
  - 总分大于 0 的书参与排序，**按总分降序**，同分可再参考阅读量。
- **未实现**：用户—用户相似度、物品—物品相似度、评分矩阵、隐式反馈矩阵分解等**经典协同过滤**核心步骤。

**论文撰写注意：**

- 第 2 章若大段写「协同过滤」并暗示**本系统已实现**，易与答辩不符；建议改为：协同过滤作为**背景与相关工作**简述，**本节实现**单独写「基于标签权重的混合打分」等表述（见下栏替换稿）。
- 摘要、第 5 章「推荐模块」须与上述逻辑一致，避免写「改进的 Item-CF」除非代码中确有对应模块。

**待完成（用户级标签权重动态更新——后续开发可落地，与论文「展望」一致）：**

以下为与当前技术栈（Spring Boot + MyBatis + 现有收藏/评论/阅读历史）**兼容的实现要点**，供以后编码时按项勾选：

- [ ] **数据层**：新增表（建议）`user_tag_weight`，字段至少包含 `user_id`、`tag_name`（或 `tag_id`）、`weight`、`updated_at`；对 `(user_id, tag_name)` 建唯一约束；初值可为 0，或首次写入时用 `tag.recommend_weight` 作为冷启动参考。
- [ ] **权重更新触发点**：在**添加收藏**、**发表评论**、**写入/更新阅读历史**等已有业务成功后，根据对应小说的 `label` 解析标签集合，调用统一服务 `UserTagWeightService`（命名可自定）对命中标签做**增量更新**（如 +Δ，需设**单日上限、总上下限**，避免刷行为导致权重爆炸）。
- [ ] **推荐侧**：`RecommendController` 中构造 `tagWeights` 时，由「仅 `TagService.getRecommendWeights()`」改为 **合并**：`effective(tag) = g( userWeight(tag), globalRecommendWeight(tag) )`，`g` 可取加权求和、取较大值等（定稿一种即可）；无用户级记录时回退为全站权重。
- [ ] **可选**：后台配置「增量 Δ、上下限、是否启用衰减」；或仅第一版写死在 `application.yml`。
- [ ] **前端/个人中心（可选）**：展示「我的标签权重」或仅后台调试接口，非必须。

**论文表述建议：** 摘要与研究内容中宜用「基于标签的混合推荐」「用户行为驱动的权重优化（待实现）」等，避免写「已实现完整协同过滤矩阵」。

**相关代码位置：**  
当前：`RecommendController.java`、`TagService.getRecommendWeights()`、`UserPreferenceService`、表 `tag`、`recommend_comment_config`。  
后续将扩展：收藏与评论、阅读历史相关 Service/Mapper，以及新建 `UserTagWeightMapper.xml` 等。

---

## 5. 后台用户管理：批量删除与管理员新增用户

**背景：**  
论文「用户管理模块设计」中可描述**批量删除**、**管理员新增系统用户**等能力；当前 `UserManageView.vue` / `AdminManageView.vue` 实现为：**分页列表、条件查询、单条编辑（弹窗）、启用/禁用**，**尚未提供**表格多选、批量删除入口，以及「新增用户」独立流程（新用户目前主要依赖**前台注册**）。

**待完成：**

- [ ] **批量删除**：在用户列表（及必要时管理员列表）增加行勾选或全选；后端提供批量删除或批量更新 `status`/逻辑删除接口；删除前二次确认；注意 `user` 与收藏、评论、阅读历史等表的**外键约束**，优先采用**禁用账号**或**逻辑删除**，避免脏数据。
- [ ] **管理员新增用户**：后台提供「新增用户」按钮与表单（至少：用户名、初始密码、邮箱/手机可选、`is_admin`、默认 `status`）；提交后调用后端 **POST** 接口写入 `user` 表，密码经 **BCrypt** 加密，用户名唯一性校验与注册接口一致。
- [ ] （可选）**批量导入**：Excel/CSV 导入用户，用于测试数据或运营批量开户，需单独做校验与回滚策略。

**相关代码位置：**  
`vue/src/views/admin/UserManageView.vue`、`AdminManageView.vue`；`springboot/.../controller/AdminUserController.java`（或同类 Admin 接口）、`UserMapper` / `UserService`。

---

## 6. （以下为占位，可自行追加）

在此继续追加其他待办项。
