# 轻小说阅读与推荐系统 — 设计与实现说明（毕业论文用）

> 本文档基于当前仓库代码与数据库脚本整理，可直接作为毕业论文中「系统概述」「需求分析」「总体设计」「数据库设计」「推荐算法」「实现要点」等章节的素材，请按学校格式自行删减与改写。

---

## 1. 项目概述

### 1.1 课题背景与目标

本系统是一个面向轻小说内容的 **Web 阅读与个性化推荐平台**，采用前后端分离架构：用户可在浏览器中浏览书库、查看详情、在线阅读、收藏与评论；系统根据用户偏好标签、平台标签权重、评论热度与阅读历史等多源信息生成推荐列表；管理员通过后台完成书籍上架（含 EPUB 解析入库）、用户与权限管理、轮播图与标签配置、推荐行为权重调参及运营数据统计。

### 1.2 系统边界

- **包含**：用户注册/登录（含手机号验证码登录）、小说检索与分类、阅读器与阅读进度、收藏、评论、完读标记、基于标签与行为的推荐、管理端 CRUD 与统计。
- **不包含或弱化**：支付、社交图谱、分布式实时推荐集群；推荐为服务端单机实时计算，适合毕业设计规模演示。

---

## 2. 技术架构

### 2.1 总体结构（B/S + 前后端分离）

```mermaid
flowchart LR
  subgraph client [浏览器]
    Vue[Vue 3 单页应用]
  end
  subgraph server [应用服务器]
    Boot[Spring Boot REST API]
    MyBatis[MyBatis ORM]
  end
  DB[(MySQL novel_db)]
  Vue -->|HTTP /api 代理| Boot
  Boot --> MyBatis --> DB
```

- **表现层**：Vue 3 + Vite + Element Plus，路由与页面状态管理，Axios 请求后端。
- **业务与数据访问层**：Spring Boot 提供 REST 接口；MyBatis XML 映射 SQL。
- **数据层**：MySQL 8.x，字符集 `utf8mb4`。

### 2.2 技术选型一览

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端运行时/构建 | Node.js 20.19+ 或 22.12+；Vite 7.x | 开发与打包 |
| 前端框架 | Vue 3（组合式 API） | 组件化 UI |
| UI 组件 | Element Plus | 表格、表单、布局等 |
| 后端 | Java 21；Spring Boot 3.2.5 | 主框架 |
| 持久层 | MyBatis Spring Boot Starter | SQL 与 Java 映射 |
| 数据库 | MySQL 8.0+ | 关系型存储 |
| 安全相关 | Spring Security Crypto（BCrypt）；JJWT | 密码存储与 Token |
| API 文档 | SpringDoc OpenAPI | 可选访问 Swagger UI |
| EPUB 处理 | Apache Commons Compress（ZIP 解压）+ JDOM2（XML） | 解析 EPUB 内资源 |

### 2.3 工程目录（摘要）

```
项目根目录/
├── vue/                      # 前端
│   ├── src/views/            # 页面（首页、书库、详情、阅读、推荐、个人中心、admin/*）
│   ├── src/components/       # 公共组件（导航、轮播、卡片等）
│   ├── src/api/              # auth / novel / behavior 等接口封装
│   └── vite.config.js        # 开发服务器与 /api 代理
├── springboot/               # 后端
│   └── src/main/java/com/wangrui/springboot/
│       ├── controller/       # REST 控制器
│       ├── service/          # 业务逻辑
│       ├── mapper/           # MyBatis 接口
│       └── pojo/             # 实体
│   └── src/main/resources/
│       ├── application.yml   # 数据源、端口、上传目录等
│       └── mapper/*.xml      # MyBatis SQL
├── novel_db.sql              # 全库表结构与初始数据（权威来源）
└── sql/                      # 增量脚本（如历史库补字段）
```

---

## 3. 功能模块说明

### 3.1 用户端

| 模块 | 功能要点 | 主要实现位置（参考） |
|------|----------|----------------------|
| 认证 | 用户名密码登录、注册、手机号+验证码登录；JWT 与用户信息本地存储 | `AuthController`，`vue/src/views/LoginView.vue` 等 |
| 书库与检索 | 小说列表、按标签筛选、详情与封面 | `NovelBookController`（`/novel`），`LibraryView.vue`、`BookDetailView.vue` |
| 阅读 | 分卷正文展示；阅读历史、进度、时长上报 | `UserBehaviorController` 阅读相关接口，`BookReadView.vue` |
| 书架与行为 | 收藏增删查；完读标记；评论增删改查 | `AuthController` 收藏接口；`UserBehaviorController` |
| 推荐 | 按「收藏衍生标签」或「自定义偏好标签」拉取推荐列表 | `RecommendController`，`RecommendView.vue` |
| 个人中心 | 用户信息、行为数据展示与配置 | `UserCenterView.vue` |

**路由与权限**：`vue/src/router/index.js` 中配置公开路由与需登录路由；后台 `/admin/*` 需 `userInfo.isAdmin === 1`。

### 3.2 管理端

| 模块 | 功能要点 |
|------|----------|
| 仪表盘/统计 | 书籍数、用户数、总阅读量、总收藏数等（`/api/admin/stats/overview`） |
| 书籍上架 | EPUB 多文件上传、ZIP 解压解析、主表与分卷入库；封面上传至配置目录 |
| 书籍/用户/管理员 | 列表与维护（具体以 `AdminNovelController`、`AdminUserController` 为准） |
| 标签与推荐权重 | 平台标签及 `recommend_weight`；标签参与推荐打分 |
| 推荐行为配置 | 表 `recommend_comment_config`：评论是否参与、条数上限与系数；阅读历史是否参与、最近 N 条与加分系数 |
| 轮播图 | `banner_carousel`：图片、链接、排序、时段、点击统计等 |

---

## 4. 数据库设计（核心表）

主脚本：`novel_db.sql`。以下为核心实体关系说明，论文中可配 **E-R 图**。

| 表名 | 作用 |
|------|------|
| `novel_book_main` | 小说主表：书名、作者、标签串 `label`、封面、阅读量、上下架 |
| `novel_book_volume` | 分卷/章节：`main_book_id` 外键，`content` 存正文 |
| `tag` | 平台标签：`name` 唯一，`recommend_weight` 参与推荐 |
| `user` | 用户：BCrypt 密码、`is_admin`、`status` |
| `user_collection` | 用户收藏 |
| `user_preference_tag` | 用户偏好标签（含 `collection` / `custom` 等类型） |
| `user_reading_history` | 阅读历史：进度、时长、`last_read_time` |
| `user_finished_novel` | 完读记录 |
| `user_comment` | 评论 |
| `banner_carousel` | 首页轮播 |
| `recommend_comment_config` | **单行**推荐行为权重配置（id 固定为 1） |

**与用户画像、推荐相关的表**：`user_preference_tag`、`user_comment`、`user_reading_history`、`tag`、`novel_book_main.label`。

---

## 5. 推荐算法说明（论文核心可展开）

### 5.1 思路概述

实现类 **基于内容的标签匹配**，并融合 **评论热度** 与 **近期阅读历史**，属于轻量级混合策略，便于在单库、单机环境下实时计算。

- **用户侧输入**：取用户前若干条偏好标签（最多参与前 5 个），标签来源分为两种模式：  
  - `sortType = collection`：由收藏行为衍生的标签序列；  
  - 其他：按 `tagType` 查询自定义等偏好标签。  
- **标签权重**：从 `tag` 表读取各标签 `recommend_weight`；若某标签在库中无配置，使用默认系数（代码中 `DEFAULT_TAG_WEIGHT = 0.1`）。
- **图书侧**：`novel_book_main.label` 为逗号/中文逗号分隔的标签串，与用户标签名匹配则累加对应权重。

### 5.2 评分公式（与实现一致）

对每本候选书计算：

\[
\text{tagScore} = \sum_{t \in T_{\text{book}} \cap T_{\text{user}}} w(t)
\]

其中 \(T_{\text{user}}\) 为参与打分的用户标签集合（至多 5 个），\(w(t)\) 为标签权重。

评论加分（可开关）：

\[
\text{commentBoost} = \min(N_{\text{comment}}, N_{\text{cap}}) \times \alpha
\]

\(N_{\text{comment}}\) 为该小说评论条数（全库聚合）；\(N_{\text{cap}}\)、\(\alpha\) 来自 `recommend_comment_config`。

阅读历史加分（可开关）：取该用户最近 \(L\) 条阅读记录（按 `last_read_time`），得到去重后的 `novel_id` 集合；若当前候选书 id 命中集合，则加固定分 \(\beta\)（每书至多加一次）。

\[
\text{totalScore} = \text{tagScore} + \text{commentBoost} + \text{readHistoryBoost}
\]

### 5.3 排序与截断

- 仅保留 `totalScore > 0` 的图书。  
- 按 `totalScore` **降序**；得分相同时按 `readCount`（阅读量）**降序**。  
- 返回前 **20** 条。

### 5.4 设计说明（可写进「算法特点与不足」）

- **优点**：实现清晰、可解释性强；后台可配置评论与阅读历史权重，便于实验与答辩演示。  
- **局限**：非协同过滤矩阵分解；冷启动依赖用户先收藏或设置标签；未单独对「收藏」加权（设计说明：收藏已通过标签偏好进入主流程）。

**代码依据**：`RecommendController#recommendBooks`（`GET /api/recommend/books`）。

---

## 6. 后端接口分层（论文「系统实现」可列表）

| 前缀/控制器 | 职责 |
|-------------|------|
| `/auth` | 登录、注册、验证码、收藏列表等 |
| `/novel` | 小说列表、标签、详情、章节内容、轮播等（经 Vite 代理为 `/api/novel`） |
| `/api/user/behavior` | 偏好标签、阅读历史、完读、评论 |
| `/api/recommend` | 推荐列表 |
| `/api/admin` | 管理端书籍、用户、标签、统计、封面上传、EPUB 上传等 |
| `/api/admin/banner` | 轮播图管理 |

统一响应结构见 `Result` 类；跨域在 `WebConfig` 中对开发端口 `5173` 配置。

---

## 7. EPUB 上架实现要点

- 管理端上传 EPUB 后，后端使用 **ZIP 流**读取 EPUB（本质为 ZIP），解析出文本与元数据写入 `novel_book_main` 与 `novel_book_volume`。  
- 封面可单独上传至 `upload.covers-dir` 指定目录，数据库保存文件名；前端通过配置的封面 URL 访问。  
- 详细操作步骤见仓库内 `EPUB上传功能说明.md`（注意：该文档中若提及 epublib，以 **实际代码** 使用的 Commons Compress + JDOM 为准）。

---

## 8. 部署与运行（环境复现）

| 项 | 默认值/说明 |
|----|-------------|
| MySQL | 库名 `novel_db`，执行 `novel_db.sql` |
| 后端端口 | `8081`（`application.yml`） |
| 前端开发端口 | `5173`，`/api` 代理到后端 |
| JDK | 21 |
| 旧库升级 | 若缺 `recommend_comment_config` 或字段不全，见 `sql/` 下脚本与 `README.md` |

生产环境可将 `vue/dist` 置于 Nginx，反向代理 `/api`、`/cover` 等到后端。

---

## 9. 论文章节映射建议

| 论文章节 | 本文档对应节 |
|----------|----------------|
| 绪论 / 研究意义 | 第 1 章 |
| 相关技术 | 第 2.2 节 |
| 需求分析 | 第 3 章（可拆用例图、用例表） |
| 总体设计 / 架构图 | 第 2.1、2.3 节 + 模块图 |
| 数据库设计 | 第 4 节 + `novel_db.sql` |
| 推荐算法设计与实现 | 第 5 节 + 源码 |
| 系统实现与界面 | 第 3、6、7 节 + 截图 |
| 测试 | 需另补测试用例（功能、接口、推荐场景） |
| 总结与展望 | 算法局限与扩展（协同过滤、离线召回等） |

---

## 10. 文档版本

- **对应仓库**：轻小说推荐系统（Vue 3 + Spring Boot + MySQL）。  
- **生成说明**：结构与业务描述依据 `README.md`、`novel_db.sql` 及 `springboot`、`vue` 源码归纳；若后续代码变更，请以实际接口与表结构为准更新本文档。
